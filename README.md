
# المساعد الشخصي الذكي - دليل المشروع

## 📝 نظرة عامة

هذا المشروع هو تطبيق ويب تفاعلي مصمم للعمل على الشاشات الصغيرة جدًا. يوفر واجهتين رئيسيتين: واجهة دردشة مع نموذج ذكاء اصطناعي (Gemini)، وواجهة لعرض معلومات شخصية محددة مسبقًا مع نظام بحث ذكي.

تم بناء التطبيق باستخدام React و TypeScript، مع استخدام Tailwind CSS للتصميم.

---

## ✨ الميزات الرئيسية

- **دردشة ذكية:** تواصل مباشر مع Gemini API لفهم الصور والنصوص.
- **سلوك AI مخصص:** تم تزويد الذكاء الاصطناعي بمجموعة صارمة من القواعد لضمان تقديم إجابات موثوقة ومبنية على الحقائق.
- **قسم معلوماتي:** قاعدة بيانات محلية قابلة للبحث بسهولة عن معلومات وروابط محددة (قنوات تيليجرام، يوتيوب، إلخ).
- **بحث ذكي:** يفهم البحث عن المرادفات والكلمات المفتاحية المختلفة (مثلاً: "يوتيوب" و "youtube" تعطي نفس النتيجة).
- **دعم الملفات:** إمكانية إرسال الصور للتحليل، وترجمة ملفات PDF و Word (DOCX).
- **تصميم متجاوب:** مصمم خصيصًا للشاشات الصغيرة جدًا مع خطوط وأيقونات مناسبة.
- **جاهز للنشر:** متوافق مع خدمات الاستضافة مثل GitHub Pages.

---

## 📁 هيكل الملفات وشرح الكود

```
.
├── README.md                 # ملف التعليمات هذا
├── index.html                # نقطة الدخول الرئيسية للتطبيق (HTML)
├── index.tsx                 # نقطة دخول React، حيث يتم ربط التطبيق بالـ DOM
├── metadata.json             # بيانات وصفية عن التطبيق
├── App.tsx                   # المكون الرئيسي الذي يدير التنقل بين الواجهات
├── types.ts                  # تعريفات TypeScript للأنواع المستخدمة في التطبيق
├── constants.ts              # يحتوي على الثوابت مثل تعليمات النظام والبيانات الشخصية
│
├── components/
│   ├── ApiKeyInput.tsx       # واجهة إدخال مفتاح API في البداية
│   ├── ChatView.tsx          # مكون واجهة الدردشة مع الذكاء الاصطناعي
│   ├── PersonalInfoView.tsx  # مكون واجهة "معلوماتي" والبحث فيها
│   └── Icons.tsx             # مكونات الأيقونات (SVG) المستخدمة في التطبيق
│
└── services/
    ├── geminiService.ts      # خدمة للتواصل وإرسال الطلبات إلى Gemini API
    └── documentProcessor.ts  # خدمة لمعالجة الملفات (استخراج النص، إنشاء ملفات جديدة)
```

### شرح تفصيلي للملفات

- **`index.html`**:
  - **الوظيفة:** هو الملف الأول الذي يتم تحميله. يحتوي على إعدادات الصفحة الأساسية، ويستدعي مكتبات خارجية لمعالجة الملفات (PDF.js, Mammoth.js, jsPDF)، ويقوم بتحميل التطبيق عبر `<script type="module" src="./index.tsx"></script>`.
  - **ملاحظة:** تم استخدام المسار النسبي (`./index.tsx`) لضمان التوافق مع GitHub Pages.

- **`App.tsx`**:
  - **الوظيفة:** هو المكون الأب. يدير الحالة الرئيسية للتطبيق مثل `apiKey` و `view` (الواجهة الحالية: دردشة أم معلوماتي). يعرض واجهة إدخال المفتاح إذا لم يتم توفيره، أو يعرض الواجهة الرئيسية مع شريط التنقل السفلي.

- **`constants.ts`**:
  - **الوظيفة:** ملف مركزي لتخزين البيانات الثابتة.
  - **`SYSTEM_PROMPT`**: يحتوي على التعليمات الصارمة التي تحدد شخصية وسلوك الذكاء الاصطناعي. **أي تعديل هنا يغير طريقة رد الـ AI.**
  - **`PERSONAL_DATA_STRUCTURED`**: مصفوفة من الكائنات تحتوي على جميع المعلومات الشخصية (الروابط، القنوات). البحث في قسم "معلوماتي" يعتمد كليًا على هذه البيانات.

- **`types.ts`**:
  - **الوظيفة:** يحدد أشكال البيانات المستخدمة في التطبيق لضمان سلامة الأنواع.
  - **`ChatMessage`**: يصف شكل كائن الرسالة في الدردشة.
  - **`PersonalInfoItem`**: يصف شكل كائن المعلومات في `PERSONAL_DATA_STRUCTURED`.

- **`components/ChatView.tsx`**:
  - **الوظيفة:** يدير كل ما يتعلق بواجهة الدردشة: عرض الرسائل، حقل الإدخال، إرفاق الملفات، إظهار مؤشر التحميل، والتواصل مع `geminiService` و `documentProcessor`.

- **`components/PersonalInfoView.tsx`**:
  - **الوظيفة:** يدير واجهة "معلوماتي". يحتوي على منطق البحث المحلي الذي يقوم بتصفية البيانات من `PERSONAL_DATA_STRUCTURED` بناءً على إدخال المستخدم وعرضها بشكل منظم مع أيقونات.

- **`services/geminiService.ts`**:
  - **الوظيفة:** يحتوي على دالة `generateContent` المسؤولة عن بناء الطلب وإرساله إلى Gemini API باستخدام المفتاح الذي أدخله المستخدم.

- **`services/documentProcessor.ts`**:
  - **الوظيفة:** يحتوي على دوال مساعدة لمعالجة الملفات باستخدام المكتبات الخارجية التي تم استدعاؤها في `index.html`. يقوم باستخراج النص من الملفات وإنشاء ملفات PDF أو TXT جديدة.

---

## 🔧 كيفية التعديل

### 1. لتغيير سلوك أو شخصية الذكاء الاصطناعي:

- اذهب إلى ملف `constants.ts`.
- قم بتعديل النص الموجود داخل المتغير `SYSTEM_PROMPT`.
- هذا هو "العقل" الذي يوجه كل ردود الذكاء الاصطناعي. يمكنك إضافة قواعد جديدة أو تعديل القواعد الحالية.

### 2. لإضافة أو تعديل قنواتك وروابطك:

- اذهب إلى ملف `constants.ts`.
- ابحث عن المتغير `PERSONAL_DATA_STRUCTURED`.
- هذه مصفوفة من الكائنات. لإضافة عنصر جديد، قم بنسخ أحد الكائنات الموجودة وتعديل قيمه:
  - `name`: الاسم الذي سيظهر في الواجهة.
  - `url`: الرابط.
  - `category`: نوع الرابط (مهم لعرض الأيقونة الصحيحة). القيم المتاحة: `telegram`, `youtube`, `tiktok`, `telegram-folder`.
  - `keywords`: **(مهم جدًا)** قائمة بالكلمات التي سيتم البحث عنها. أضف كل الكلمات المحتملة التي قد يستخدمها شخص للبحث عن هذا العنصر (مثلاً: `['gaming', 'games', 'العاب', 'جيمينج']`).

**مثال لإضافة حساب فيسبوك:**
```javascript
{ 
  name: 'صفحة الفيسبوك', 
  url: 'https://facebook.com/your-page', 
  category: 'telegram', // يجب إضافة أيقونة جديدة ونوع جديد إذا أردت تمييزه
  keywords: ['facebook', 'فيسبوك', 'فيس'] 
}
```
> **ملاحظة:** ستحتاج إلى إضافة أيقونة فيسبوك إلى `components/Icons.tsx` وتحديث دالة `getIcon` في `components/PersonalInfoView.tsx` لدعم الفئة الجديدة.

### 3. لتغيير التصميم والألوان:

- يستخدم التطبيق Tailwind CSS. يمكنك تغيير أسماء الأصناف مباشرة في ملفات `tsx` لتغيير المظهر.
- **الألوان الرئيسية:** ابحث عن `cyan` في المشروع (مثل `bg-cyan-600`, `text-cyan-400`) واستبدلها باللون الذي تفضله من لوحة ألوان Tailwind.
- **الخطوط والأحجام:** يتم التحكم بها أيضًا عبر أصناف Tailwind مثل `text-xs`, `text-sm`.

### 4. لتغيير الأيقونات:

- اذهب إلى ملف `components/Icons.tsx`.
- يمكنك استبدال كود الـ SVG لأي أيقونة موجودة.
- لإضافة أيقونة جديدة، قم بإنشاء مكون جديد بنفس الطريقة وأضفه إلى الملف. لا تنس استيراده في المكان الذي تريد استخدامه فيه.
